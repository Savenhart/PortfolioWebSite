import { CarouselItemDirective } from './carousel-item.directive';

describe('CarouselItemDirective', () => {
  it('should create an instance', () => {
    const directive = {} as CarouselItemDirective;
    expect(directive).toBeTruthy();
  });
});
